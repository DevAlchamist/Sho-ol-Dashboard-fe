/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["tailus.io", "i.imgur.com", "via.placeholder.com"], // Add any other domains as necessary
  },
};

export default nextConfig;
